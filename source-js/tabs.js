const data={TABS:"tab",TAB:"tab__link"},changeTab=function(a){const t=document.querySelector(`.${data.TABS}--${a}`);Array.prototype.slice.call(document.querySelectorAll(`.${data.TABS}`)).forEach((a=>{a.style.display="none"})),t.style.display="block"};document.addEventListener("click",(a=>{a.target.classList[0]===data.TAB&&changeTab(a.target.classList[1].slice(-1))})),changeTab(0);