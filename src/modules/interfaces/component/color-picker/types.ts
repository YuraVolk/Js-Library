import { RGBAColor } from "../../generic/colors";

export interface ColorPickerConfiguration {
    imageUrl: string;
    width: number;
    height: number;
    backgroundColor: RGBAColor;
    rgba: RGBAColor;
}
